EAPI="5"

inherit pax-utils git-r3

	EGIT_SUB_PROJECT="core"
	EGIT_URI_APPEND="${PN}"
	EGIT_REPO_URI="https://aur.archlinux.org/tor-browser-en.git"

SLOT="0"

src_prepare() {
	default
}

src_configure() {
	default
}

src_compile() {
	default
}

src_install() {
	default
}
